<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /cyra-connect/

    # Route all requests into the Laravel public directory
    RewriteRule ^public/ - [L]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
